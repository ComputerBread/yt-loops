

const loops = [];
// one loop = {enable, start, end};

addLoop() {
	loops.push({enable: true, start, end});
}

clearLoop() {
	start = 0;
	end = 0;
}

removeLoop(idx) {

}

toggleLoop(idx) {
}

startLoops() {
}


